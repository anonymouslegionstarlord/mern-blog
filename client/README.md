# MERN Blog Client

React 19, Vite, and Tailwind CSS frontend for the repository's Express API. Set `VITE_API_URL` when the API is not available at `http://localhost:5000/api`.

```bash
npm install
npm run dev
```
